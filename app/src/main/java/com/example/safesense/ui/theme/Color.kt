package com.safesense.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary Reds ────────────────────────────────────────────────────────────
val PrimaryRed   = Color(0xFFD32F2F)   // Main brand red
val DeepRed      = Color(0xFFB71C1C)   // Dark red / hover states
val MidRed       = Color(0xFFEF5350)   // Lighter red / accents
val RedLight     = Color(0xFFFFEBEE)   // Red tint backgrounds
val RedSoft      = Color(0xFFFFCDD2)   // Red tint borders

// ─── Neutrals ─────────────────────────────────────────────────────────────────
val White        = Color(0xFFFFFFFF)
val OffWhite     = Color(0xFFFFF8F8)   // Warm white background
val Gray100      = Color(0xFFF5F5F5)   // Lightest gray / card bg
val Gray200      = Color(0xFFEEEEEE)   // Dividers / borders
val Gray400      = Color(0xFFBDBDBD)   // Inactive / placeholder
val Gray600      = Color(0xFF757575)   // Secondary text
val Gray800      = Color(0xFF424242)   // Dark text / alt avatars
val Gray900      = Color(0xFF212121)   // Primary text / headings

// ─── Semantic ─────────────────────────────────────────────────────────────────
val SuccessGreen  = Color(0xFF2E7D32)  // Walk Mode active / "I'm Safe" button
val SuccessLight  = Color(0xFFE8F5E9)  // Green tint backgrounds
val WarningAmber  = Color(0xFFF57F17)  // Sensor warning state
val WarningLight  = Color(0xFFFFF8E1)  // Amber tint backgrounds

// ─── Special Usage ────────────────────────────────────────────────────────────
val CountdownBg   = Color(0xFFB71C1C)  // Countdown overlay — full screen deep red
val PulseDot      = Color(0xFF69F0AE)  // Monitoring active indicator — bright green
val OnboardingBg  = Color(0xFFD32F2F)  // Onboarding hero background
val NearBlack     = Color(0xFF1A1A1A)  // App dark background (mockup) / "Safe" wordmark
val SloganGray    = Color(0xFFBBBBBB)  // Slogan / subtitle text